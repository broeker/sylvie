{% extends "base.njk" %}
{% block content %}
  <h1 class="text-3xl font-bold">{{ title }}</h1>
n

  {% if image %}
    <img src="{{ image }}" alt="{{ title }}" style="max-width: 100%; height: auto;">
  {% endif %}

  {{ content | safe }}
{% endblock %}
